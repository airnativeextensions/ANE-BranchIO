<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="@ANDROIDPACKAGE@" >

    <uses-sdk android:minSdkVersion="16" />

    <uses-permission android:name="com.google.android.finsky.permission.BIND_GET_INSTALL_REFERRER_SERVICE" />

    <application>

        <!--
        <activity android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <intent-filter>
                <data android:scheme="${branchAndroidScheme}" android:host="open" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
            </intent-filter>
        </activity>
        -->

        <meta-data android:name="io.branch.sdk.TestMode" android:value="${branchTestMode}" />
        <meta-data android:name="io.branch.sdk.BranchKey" android:value="${branchAndroidKey}" />
        <meta-data android:name="io.branch.sdk.BranchKey.test" android:value="${branchAndroidTestKey}" />

     </application>

</manifest>